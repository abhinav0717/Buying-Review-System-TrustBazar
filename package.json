{
  "name": "trustbazaar-capstone",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack rating, review, and real-time chat marketplace capstone project.",
  "scripts": {
    "install:all": "npm.cmd install --prefix server && npm.cmd install --prefix client",
    "dev": "concurrently \"npm.cmd run dev --prefix server\" \"npm.cmd run dev --prefix client\"",
    "build": "npm.cmd run build --prefix client",
    "seed": "npm.cmd run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}

